# Pytorch MNIST example

## Dependencies

* `torch`
* `torchvision`

You can install pytorch using [Anaconda](https://www.anaconda.com/products/individual): `conda install pytorch torchvision cpuonly -c pytorch`

## Running

```shell
conda run python main.py
```
